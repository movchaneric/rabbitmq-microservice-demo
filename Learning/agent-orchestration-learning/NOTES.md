# Notes

- Hands-on rule applies here too (see `[[feedback_hands_on_learning]]`), but in this track
  it means: the user runs every `git`/`gh`/`claude` CLI command and writes every real
  skill/config file (`SKILL.md`, agent frontmatter, PRD/issue text) themselves. I write
  lesson HTML + reference material and guide the blanks interactively.
- Practice vehicle is a **fresh, separate repo** the user creates during Lesson 1 (name/
  location is their call) — do not practice worktree/pipeline mechanics inside
  `rabbitmq-microservice-demo` itself. This `Learning/` workspace only holds the lessons,
  records, and mission — not the practice repo's code.
- Baseline (2026-08-06): new to both git worktrees and Claude Code subagents/Task tooling —
  don't assume either, even though the user is comfortable with git/TS/Express from the
  other tracks in this repo.
- User explicitly wants to move past "prompt -> work -> prompt -> work" — frame every
  lesson against that contrast: what does the orchestrated version get you that serial
  prompting doesn't?
- Motivation is mixed: resume-truth + a real personal tool + possible job requirement.
  Don't over-index on "for the resume" language inside lessons — keep it grounded in the
  mechanics being genuinely useful day to day.
