# Lesson 1 (git worktree fundamentals) complete

Completed 2026-08-06. Practice repo created at `/Users/ericmovchan/agent-pipeline-practice`
(sibling to `rabbitmq-microservice-demo`, per the mission's constraint to keep hands-on
pipeline work out of this repo). User ran `git worktree add`/`list`/`remove` themselves and
confirmed the core claim — one shared `.git` history across linked worktrees — rather than
just reading about it. Lesson 2 (Claude Code subagents) and everything downstream that
references "the practice repo" means this path.
