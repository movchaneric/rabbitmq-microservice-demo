# Lesson 3 (worktree-isolated worker agents) complete

Completed 2026-08-07 in `/Users/ericmovchan/agent-pipeline-practice` — confirmed directly
after a mixup where the user first ran `git worktree list` in
`rabbitmq-microservice-demo` (this repo, correctly showing no worktrees, since practice
work is deliberately kept out of here) rather than the practice repo. Worth double-checking
completion when a command turns up in the wrong repo, rather than assuming "let's move on"
alone means the hands-on step happened — this time it had, but the check was warranted.
